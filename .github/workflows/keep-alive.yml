name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Salez Girl
        run: curl --max-time 120 https://salez-girl.onrender.com/
