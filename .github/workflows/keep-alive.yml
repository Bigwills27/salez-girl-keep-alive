name: Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Ping Salez Girl
        run: curl --max-time 120 https://salez-girl.onrender.com/

      - name: Ping Andy Alert
        run: curl --max-time 120 https://andy-alert.onrender.com/
